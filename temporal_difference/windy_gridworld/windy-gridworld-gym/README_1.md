### Windy GridWorld

This repository hosts the TD algorithm to train an agent on solving a gridworld problem with fixed or stochastic windy influence. Implemented in a gymnasium environment.

## Installation

To install this environment, run the following commands:

```{shell}
cd gymnasium_env
pip install -e .
```
